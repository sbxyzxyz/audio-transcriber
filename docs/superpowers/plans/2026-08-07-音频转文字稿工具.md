# 音频/视频转文字稿工具 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做一个本地免费运行的图形界面工具，把视频/音频文件拖进去点一下，输出带时间戳的中文文字稿。

**Architecture:** 三层——tkinter 图形界面（app.py）+ 转写核心（transcriber.py，封装 faster-whisper）+ 纯函数工具（formatter.py，格式化时间戳/文字稿）。界面调核心，核心返回带时间的句子列表，由格式化函数拼成文字稿。

**Tech Stack:** Python 3.12（已装）、tkinter（已确认可用）、faster-whisper（转写引擎，支持 CUDA）、ffmpeg（已装，faster-whisper 依赖它解码音频）。

**项目位置:** `C:\Users\sbxyz\转文字稿工具\`
**Git:** 该目录尚无 git 仓库。计划中包含初始化。

---

## 文件结构

```
C:\Users\sbxyz\转文字稿工具\
├── app.py               # tkinter 图形界面（主入口）
├── transcriber.py       # 转写核心：封装 faster-whisper
├── formatter.py         # 纯函数：时间戳格式化、文字稿拼装（可测试）
├── requirements.txt     # 依赖清单
├── 启动工具.bat         # 双击启动脚本
├── test_formatter.py    # 纯函数单元测试
└── docs\
    └── superpowers\
        └── specs\2026-08-07-音频转文字稿工具-design.md   # 设计文档（已完成）
```

---

### Task 1: 初始化项目（venv + 依赖 + git）

**Files:**
- Create: `C:\Users\sbxyz\转文字稿工具\requirements.txt`

- [ ] **Step 1: 创建 requirements.txt**

```txt
faster-whisper>=1.0.0
tkinterdnd2>=0.4.0
pytest>=8.0.0
```

- [ ] **Step 2: 创建虚拟环境并安装依赖**

Run（在 `C:\Users\sbxyz\转文字稿工具\` 下）:
```bash
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt
```
Expected: pip 安装完成，无报错。

- [ ] **Step 3: 初始化 git 仓库**

```bash
cd /c/Users/sbxyz/转文字稿工具
git init
git add requirements.txt
git commit -m "chore: 初始化项目与依赖清单"
```

- [ ] **Step 4: 验证 faster-whisper 可用**

```bash
.venv/Scripts/python -c "import faster_whisper; print(faster_whisper.__version__)"
```
Expected: 打印版本号。

---

### Task 2: formatter.py — 纯函数模块

**Files:**
- Create: `C:\Users\sbxyz\转文字稿工具\formatter.py`
- Test: `C:\Users\sbxyz\转文字稿工具\test_formatter.py`

- [ ] **Step 1: 写失败的测试**

```python
# test_formatter.py
from formatter import format_timestamp, format_transcript


def test_format_timestamp():
    assert format_timestamp(0) == "[00:00]"
    assert format_timestamp(5) == "[00:05]"
    assert format_timestamp(65) == "[01:05]"
    assert format_timestamp(3605) == "[60:05]"


def test_format_transcript():
    segments = [
        {"start": 0.0, "text": "大家好"},
        {"start": 5.2, "text": "欢迎来到我的频道"},
    ]
    assert format_transcript(segments) == (
        "[00:00] 大家好\n"
        "[00:05] 欢迎来到我的频道"
    )
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python -m pytest test_formatter.py -v`
Expected: FAIL（ModuleNotFoundError: formatter）

- [ ] **Step 3: 写最小实现**

```python
# formatter.py
def format_timestamp(seconds: float) -> str:
    """把秒数格式化成 [mm:ss]，mm 可超过 60（长视频）。"""
    s = int(round(seconds))
    return f"[{s // 60:02d}:{s % 60:02d}]"


def format_transcript(segments: list) -> str:
    """把带 start 和 text 的句子列表拼成带时间戳文字稿。"""
    lines = [f"{format_timestamp(seg['start'])} {seg['text'].strip()}"
             for seg in segments]
    return "\n".join(lines)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/Scripts/python -m pytest test_formatter.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
git add formatter.py test_formatter.py
git commit -m "feat: 时间戳与文字稿格式化"
```

---

### Task 3: transcriber.py — 转写核心

**Files:**
- Create: `C:\Users\sbxyz\转文字稿工具\transcriber.py`

- [ ] **Step 1: 写转写核心**

```python
# transcriber.py
from faster_whisper import WhisperModel


MODEL_SIZES = {
    "极速": "small",
    "标准": "medium",
    "高精度": "large-v3",
}

LANGUAGES = {
    "中文": "zh",
    "自动识别": None,
}


class Transcriber:
    def __init__(self, model_size: str, language: str | None):
        self.model_size = MODEL_SIZES.get(model_size, "medium")
        self.language = language
        # 优先 GPU，失败回退 CPU
        try:
            self.model = WhisperModel(self.model_size, device="cuda", compute_type="float16")
        except Exception:
            self.model = WhisperModel(self.model_size, device="cpu", compute_type="int8")

    def transcribe(self, audio_path: str) -> list[dict]:
        segments_iter, _info = self.model.transcribe(
            audio_path, language=self.language, vad_filter=True
        )
        return [{"start": seg.start, "text": seg.text}
                for seg in segments_iter]
```

- [ ] **Step 2: 冒烟测试（用一条 10 秒测试音频）**

生成测试音频：
```bash
ffmpeg -f lavfi -i sine=frequency=440:duration=10 -y test_sine.wav
```

Run: `.venv/Scripts/python -c "
from transcriber import Transcriber
t = Transcriber('极速', '中文')
segs = t.transcribe('test_sine.wav')
print('segments:', len(segs))
for s in segs: print(s)
"`
Expected: 能打印 segments（纯音调可能识别为空，但程序不崩溃、能返回）。注意首次运行会下载 small 模型（约 460MB），需等待。

- [ ] **Step 3: 提交**

```bash
git add transcriber.py
git commit -m "feat: faster-whisper 转写核心（GPU/CPU 回退）"
```

---

### Task 4: app.py — 图形界面

**Files:**
- Create: `C:\Users\sbxyz\转文字稿工具\app.py`

- [ ] **Step 1: 写图形界面**

```python
# app.py
import os
import threading
import tkinter as tk
from tkinter import filedialog, messagebox, ttk

from formatter import format_transcript
from transcriber import LANGUAGES, MODEL_SIZES, Transcriber

try:
    from tkinterdnd2 import DND_FILES, TkinterDnD
    DND_AVAILABLE = True
except ImportError:
    DND_AVAILABLE = False


class App:
    def __init__(self, root):
        self.root = root
        root.title("视频转文字稿工具")
        root.geometry("560x420")

        self.file_path = None

        # 拖放区
        self.drop_label = tk.Label(
            root, text="把视频/音频文件拖到这里\n或点击下方按钮选择文件",
            bg="#e8f0fe", fg="#333333", font=("Microsoft YaHei", 12),
            relief="groove", height=5, cursor="hand2",
        )
        self.drop_label.pack(fill="both", expand=True, padx=20, pady=15)
        self.drop_label.bind("<Button-1>", lambda e: self.choose_file())
        if DND_AVAILABLE:
            self.drop_label.drop_target_register(DND_FILES)
            self.drop_label.dnd_bind("<<Drop>>", self.on_drop)
        else:
            self.drop_label.config(text="点击下方按钮选择文件")

        # 文件显示
        self.file_var = tk.StringVar(value="未选择文件")
        tk.Label(root, textvariable=self.file_var, fg="#555555",
                 font=("Microsoft YaHei", 10), wraplength=500).pack(pady=(0, 8))

        # 选项行
        opts = tk.Frame(root)
        opts.pack(pady=4)
        tk.Label(opts, text="精度:", font=("Microsoft YaHei", 10)).grid(row=0, column=0)
        self.model_var = tk.StringVar(value="标准")
        ttk.Combobox(opts, textvariable=self.model_var, width=8,
                     values=list(MODEL_SIZES), state="readonly").grid(row=0, column=1, padx=8)
        tk.Label(opts, text="语言:", font=("Microsoft YaHei", 10)).grid(row=0, column=2)
        self.lang_var = tk.StringVar(value="中文")
        ttk.Combobox(opts, textvariable=self.lang_var, width=8,
                     values=list(LANGUAGES), state="readonly").grid(row=0, column=3, padx=8)
        ttk.Button(opts, text="选择文件", command=self.choose_file).grid(row=0, column=4, padx=8)

        # 开始按钮
        self.start_btn = ttk.Button(root, text="开始转文字", command=self.start)
        self.start_btn.pack(pady=10)

        # 进度条
        self.progress = ttk.Progressbar(root, mode="indeterminate")
        self.progress.pack(fill="x", padx=40)

        # 状态栏
        self.status_var = tk.StringVar(value="就绪")
        tk.Label(root, textvariable=self.status_var, fg="#888888",
                 font=("Microsoft YaHei", 9)).pack(pady=6)

    def choose_file(self):
        path = filedialog.askopenfilename(
            title="选择视频或音频文件",
            filetypes=[("音视频文件", "*.mp4 *.mov *.mkv *.avi *.flv *.mp3 *.wav *.m4a *.aac *.flac *.ogg"), ("所有文件", "*.*")],
        )
        if path:
            self.file_path = path
            self.file_var.set(os.path.basename(path))

    def on_drop(self, event):
        raw = event.data
        # tkinterdnd2 返回的路径可能带花括号，去掉
        path = raw.strip("{}").strip()
        if os.path.isfile(path):
            self.file_path = path
            self.file_var.set(os.path.basename(path))

    def start(self):
        if not self.file_path:
            messagebox.showwarning("提示", "请先选择文件")
            return
        self.start_btn.config(state="disabled")
        self.progress.start(12)
        self.status_var.set("正在转写，请稍候…（首次运行需下载模型）")
        threading.Thread(target=self._work, daemon=True).start()

    def _work(self):
        try:
            model_size = self.model_var.get()
            language = LANGUAGES[self.lang_var.get()]
            tr = Transcriber(model_size, language)
            segments = tr.transcribe(self.file_path)
            text = format_transcript(segments)
            out_path = os.path.splitext(self.file_path)[0] + "_文字稿.txt"
            with open(out_path, "w", encoding="utf-8") as f:
                f.write(text)
            self.root.after(0, self._done, out_path)
        except Exception as e:
            self.root.after(0, self._error, str(e))

    def _done(self, out_path):
        self.progress.stop()
        self.start_btn.config(state="normal")
        self.status_var.set("完成")
        messagebox.showinfo("完成", f"文字稿已生成：\n{out_path}")

    def _error(self, msg):
        self.progress.stop()
        self.start_btn.config(state="normal")
        self.status_var.set("出错")
        messagebox.showerror("出错", msg)


def main():
    if DND_AVAILABLE:
        root = TkinterDnD.Tk()
    else:
        root = tk.Tk()
    App(root)
    root.mainloop()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 启动界面验证**

Run: `.venv/Scripts/python app.py`
Expected: 弹出窗口，含拖放区、精度/语言下拉框、开始按钮、进度条。手动检查布局无错位，关闭窗口正常退出。

- [ ] **Step 3: 提交**

```bash
git add app.py
git commit -m "feat: 图形界面（拖放/选文件/进度/完成提示）"
```

---

### Task 5: 启动脚本 + 端到端验证

**Files:**
- Create: `C:\Users\sbxyz\转文字稿工具\启动工具.bat`

- [ ] **Step 1: 写双击启动脚本**

```bat
@echo off
cd /d "%~dp0"
if not exist .venv\Scripts\python.exe (
    echo 首次运行，正在安装依赖，请稍候...
    python -m venv .venv
    .venv\Scripts\pip install -r requirements.txt
)
start "" .venv\Scripts\pythonw.exe app.py
```

- [ ] **Step 2: 端到端测试（用真实中文音频）**

准备一段中文口播音频（从用户视频里剪一段，或用任意 mp3），运行：
```bash
.venv/Scripts/python app.py
```
通过界面拖入音频 → 选「标准」+「中文」→ 开始转写。
Expected: 进度条转动，完成后弹出提示，源文件同目录生成 `*_文字稿.txt`，内容形如：
```
[00:00] 大家好，欢迎来到我的频道
[00:05] 今天给大家讲讲工地那些事
```
时间戳递增、内容正确、中文识别无误。

- [ ] **Step 3: 提交**

```bash
git add 启动工具.bat
git commit -m "feat: 双击启动脚本 + 端到端验证"
```

---

## 自检

- **设计覆盖**：界面流程（拖放/选精度/选语言/开始/进度/完成提示）→ Task 4；转写核心 GPU 加速 → Task 3；时间戳格式 `[mm:ss]` → Task 2（含 60 分钟以上长视频处理）；默认中文 → Task 4 默认值；错误处理（非音视频/引擎失败）→ Task 4 `_error`；取消 → 界面关闭即取消（daemon 线程）。设计文档中「不做 SRT/批量/云同步」→ 无对应任务，符合 YAGNI。
- **占位符扫描**：无 TBD/TODO，所有代码步骤均含完整代码。
- **类型一致性**：`format_timestamp(seconds)`、`format_transcript(segments)`（Task 2 定义）在 Task 4 `_work` 中以 `format_transcript(segments)` 调用，一致。`Transcriber(model_size, language)` 构造签名（Task 3 定义）在 Task 4 `_work` 中以 `Transcriber(model_size, language)` 调用，一致。`MODEL_SIZES`/`LANGUAGES` 字典（Task 3 定义）在 Task 4 中 import，一致。
